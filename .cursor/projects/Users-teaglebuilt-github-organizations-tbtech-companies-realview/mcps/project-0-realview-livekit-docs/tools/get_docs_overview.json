{
  "name": "get_docs_overview",
  "description": "Returns a complete overview of the docs site and its table of contents, along with page descriptions. This is a great starting point to load context for browsing conceptual docs rather than relying wholly on search.",
  "arguments": {
    "type": "object",
    "properties": {
      "format": {
        "type": "string",
        "enum": [
          "markdown",
          "json"
        ],
        "default": "markdown",
        "description": "Response format. \"markdown\" (default) returns human-readable markdown optimized for LLM consumption. \"json\" returns structured JSON for programmatic use."
      },
      "lkCliVersion": {
        "type": "string"
      },
      "projectId": {
        "type": "string"
      }
    },
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}